# What this pack found — vs what was already known

Pin **AEB2AD**. 0 free parameters. Human-facing. Courtship/aggression \(T_1\) off.

This is **FSOT applied to measured fly dumps**, not a claim that the connectome papers were wrong. Biology already knew the cell types. What was not in the literature is this **law** on those objects: leftover vs isolated command, look-split, Ledger B, hop-2 masses under residual \(a\leftarrow rWa\).

## Already known (do not re-announce as discovery)

| Fact | Where it already lived |
|------|-------------------------|
| JO / chordotonal / VNC sensory drive motor | FlyWire Male CNS, BANC papers |
| DNg29 is a descending type | type catalogs; n=2 both sexes |
| Olfactory hop stays in antennal-lobe LNs | connectome hops; il3LN6 / lLN2F |
| Kenyon / APL is an MB hub, not a walk command | hemibrain MB |
| pC1 participates in courtship and aggression | Pan 2011; Gao eLife 2024; *fru*/*dsx* |
| GABA is the main inhibitory transmitter | Male/BANC `consensus_nt` |
| No Atlas→bodyId join | unpublished |

## New in this pack (FSOT objects on measured \(W\))

These are not new neurons. They are **new numbers and a leftover taxonomy** under \(S=K(T_1+T_2+T_3)\), \(r=1+|S|P_{\mathrm{NEW}}\), overlay \(1/\varphi\), consensus trit, 0 knobs.

1. **Class-gated leftover vs isolated command (TED-10).** Chordotonal *class* n=2136 hop-2 vm **5.01** (command). **INXXX007** type-alone n=2 leftover. **DNg29** type-alone n=2 still desc **9.20**. Same n=2, opposite job. BANC `XXX` unlabeled. Do not grow the collapse target; splice class → FETi (BANC n=6, vm **9.19**). Connectomics described the pathway; it did not publish this isolate-vs-class residual split.

2. **Look-split Biochemistry vs Neuroscience |S| = 0.494% ≤ 0.5% Lean gate** (Adventure 1 fold). Nest \(D_{\mathrm{eff}}\) 10 vs 11, no fitted domain knob.

3. **Ledger B on dump N, GABA n, transmitter AA MW: 17/17, median 0.0761%** using seed \(\mathrm{ALPHA}\), not \(P_{\mathrm{NEW}}\).

4. **Safety family hop-2 if T1 were on (TED-15).** pC1 n=156 desc **30.94**; TN1 n=35 vm **10.07**; fru/dsx labeled n=5012 desc **35.21**. Community knew pC1 is a command-ish type. These residual masses on Male \(W\) under AEB2AD were not a published table. That is why default observer stays off.

5. **Two-animal function 3/3; magnitude is two animals** (sex/cut), not one measured \(m\) under the 0.5% scalar gate.

6. **Teaching trajectory is data (Adventure 1).** Hop-2 walk vs leftover, two-animal 3/3, look-split 0.494%, language/code splices, pC1 desc 30.94 if T1 on — these **held across Bills**. That is longitudinal measurement of this organism learning to read/math/code, not a throwaway accuracy score.

7. **English “times” ALU misfire (TED-16) then adaptive grammar (TED-18).** Blind **0/22** leftover + false \(\times\). Per-item schemas **22/22** (not independent retry). TED-18 grammar/dictionary **without per-item schema 22/22**, LTM bind 22. Hardcoding every leftover job is not biological; this is the remedy.

8. **Inf-norm hop masses are not extensive in \(n_{\mathrm{seed}}\)** (TBD vs 07B). APPLY wrong-object: leftover scoring ≠ classifier.

## What we are *not* claiming

- A new connectome, a new gene, or a fitted brain model.
- That FSOT is already accepted as a ToE (it is the law this pack uses; the community has not adopted it).
- That Wikipedia is an empirical dump. Web text is leftover until named as a research observer with overlay/consensus, never over \(W\).

Keep this file in sync when a TED produces a **number or leftover class** that is not already a connectome sentence.
