# Coding-token splice (TED-14)

Pin **AEB2AD**. 0 free parameters.

Coding tokens sit on the language invented region: `let`, `if`/`else`, `while` (horizon \(\mathrm{round}(\varphi^5)=11\)), trit functions. Not FlyWire \(W\). Courtship names DENY. Unknown leftover.

**41/41**. promotes=True → **Bill-14**.
